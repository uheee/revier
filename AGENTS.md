## Agent skills

### Issue tracker

本仓库的 issue 和 spec 使用 GitHub Issues 管理。详见 `docs/agents/issue-tracker.md`。

### Triage labels

使用默认的五角色 triage 标签词汇。详见 `docs/agents/triage-labels.md`。

### Domain docs

使用单上下文领域文档布局。详见 `docs/agents/domain.md`。
