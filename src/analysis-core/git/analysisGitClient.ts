import type { ChangedFile, RelatedCommit } from '../../shared/reviewTypes';
export interface BlameLine { lineNumber: number; originalLineNumber: number; commitHash: string; authorName: string; authorEmail?: string; committedAt: string; subject: string; content: string; }
export interface AnalysisGitClient { listChangedFiles(repoPath: string, baseCommit: string, headCommit: string): Promise<ChangedFile[]>; readFileAtCommit(repoPath: string, commit: string, filePath: string): Promise<string>; showFilePatch(repoPath: string, commit: string, filePath: string): Promise<string>; blameFileRange(repoPath: string, commit: string, filePath: string, startLine: number, endLine: number): Promise<BlameLine[]>; listParents(repoPath: string, commitHash: string): Promise<string[]>; getCommit(repoPath: string, commitHash: string): Promise<RelatedCommit | undefined>; }
